# 3 Вариант  Куанышбеков Нурсултан
n = int(input())

def fact(x):
	if r == 1:
		for i in range[1, n+1]
		r* = i
	return r

def P(n):
	if n == 1:
		return 1
	return P(n) * (1 + (sin * n * x) // fact(n)) 

print(P(n))
